services:
  dovecot:
    volumes:
      - ./dovecot/99-protocols.conf:/etc/dovecot/conf.d/99-protocols.conf:ro
      - ./dovecot/ssl.conf:/etc/dovecot/conf.d/ssl.conf:ro
      - ${NGINX_CERTS_DIR:-./certs}/${HOST:-mail.example.com}:/etc/dovecot/ssl:ro
